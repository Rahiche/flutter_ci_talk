name: test_bundler
description: Generate single-isolate Flutter test bundle wrappers for faster CI test runs.
version: 0.1.0
publish_to: 'none'
resolution: workspace
environment:
  sdk: ^3.9.0
executables:
  test-bundler: test_bundler
dependencies:
  args: ^2.5.0
  path: ^1.9.0
dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
