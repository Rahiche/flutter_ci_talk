import 'package:flutter/material.dart';

class ConversionFunnel extends StatelessWidget {
  const ConversionFunnel({super.key});
  @override
  Widget build(BuildContext context) {
    return const Card(child: Padding(padding: EdgeInsets.all(16), child: Text('ConversionFunnel')));
  }
}
