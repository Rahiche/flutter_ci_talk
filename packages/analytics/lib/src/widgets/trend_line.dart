import 'package:flutter/material.dart';

class TrendLine extends StatelessWidget {
  const TrendLine({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('TrendLine'));
  }
}
