name: notifications_api
description: shared package (generated by tool/scale_up.dart)
publish_to: 'none'
# Workspace siblings depend on each other by name with a ^1.0.0 constraint,
# so every generated package has to declare a version that satisfies it.
version: 1.0.0
resolution: workspace

environment:
  sdk: ^3.9.0
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.7
  collection: ^1.19.0
  intl: ^0.20.2
  core_data: ^1.0.0
  core_utils: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^10.0.0
  flutter_lints: ^5.0.0
